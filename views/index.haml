%div{style: "margin-left: auto; margin-right: auto; text-align: center"}
  %p{style: "font-size: 50px; font-family: Verdana"}
    = word
  %button{onClick: "document.location.reload(true)", style: "width: 300px; height: 50px; border-radius: 10px; -moz-border-radius: 10px;"}
    next word (or you can just refresh the page)
